# Runtime Audio Assets

These are the browser-served static audio assets used by the Prompt 30 sound manifest and playback layer.

Files in this folder are intentionally pre-baked and deterministic. They are not AI-generated at runtime.
